const express = require('express');
const router = express.Router();
const callController = require("../../controllers/callcontroller");

// router.post('/video_call', callController.createCustomer); 

module.exports = router;